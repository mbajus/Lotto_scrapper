CREATE TABLE [IF NOT EXISTS] multimulti (
  id INT PRIMARY KEY,
  num1 VARCHAR(59) NOT NULL,
  num2 VARCHAR(2),
  date INT NOT NULL,
  time INT,
  ss_id INT,
  FOREIGN KEY (ss_id)
      REFERENCES superszansa (ss_id),
);