CREATE TABLE [IF NOT EXISTS] superszansa (
  id INT PRIMARY KEY,
  num1 INT NOT NULL,
  num2 INT,
  date INT NOT NULL,
  time INT,
  ss_id INT,
  FOREIGN KEY (ss_id)
      REFERENCES superszansa (ss_id),
);